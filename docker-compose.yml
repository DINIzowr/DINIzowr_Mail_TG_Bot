services:
  mail-bot:
    build: .
    env_file: .env
    environment:
      DATABASE_URL: sqlite+aiosqlite:////app/data/mail_bot.db
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
      - ./client_secret.json:/app/client_secret.json:ro
    restart: unless-stopped
